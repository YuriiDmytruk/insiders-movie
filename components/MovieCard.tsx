import React from 'react';
import {Text, View} from 'react-native';

const MovieCard = () => {
    //const IMAGE_PATH = 'https://image.tmdb.org/t/p/original';
    //IMAGE_PASS + props.movie.poster_path
  return (
    <View>
      <Text>MovieCard</Text>
    </View>
  );
};

export default MovieCard;
